---
/**
 * Contact page — English. Adapted for English-speaking founders.
 * Keep structure + styles in sync with src/pages/contact.astro.
 *
 * Web3Forms key is wired (2026-05-19). To rotate it, replace the key below
 *   AND in src/pages/contact.astro — both pages must carry the same key.
 */
import Base from '../../layouts/Base.astro';
import { localizedUrl } from '../../lib/links';

const WEB3FORMS_ACCESS_KEY = 'acd1f7f4-6610-45b3-a0fb-4bf078dd6ef7';
const thankYouUrl = new URL(localizedUrl('/thank-you', 'en'), Astro.site).href;

const steps = [
  'You send the request — takes a couple of minutes.',
  'We ask a few qualifying questions in writing — a quick back-and-forth.',
  'If we’re a fit, we audit your ad account and send a written report.',
  'If we’re not, we tell you straight — no pitch. Where we can, we’ll point you to someone better suited.',
];
---

<Base
  title="Contact — SNC Advertising | Get an audit"
  description="Request an audit of your ad account. A short qualifying step, then a written review — if we're a fit."
>
  <section class="page-hero">
    <div class="page-hero-label">Contact</div>
    <h1 class="page-hero-h1">Get an audit.</h1>
    <p class="page-hero-lede">
      Send us a note. We'll ask a few questions to see if we're a fit. If we
      are, we'll review your ad account. If not, we'll tell you straight away.
    </p>
  </section>

  <!-- Form -->
  <section class="section section-dark">
    <div class="sec-label on-dark">Request</div>

    <form id="audit-form" class="form" action="https://api.web3forms.com/submit" method="POST" data-redirect={thankYouUrl}>
      <input type="hidden" name="access_key" value={WEB3FORMS_ACCESS_KEY} />
      <input type="hidden" name="redirect" value={thankYouUrl} />
      <input type="hidden" name="subject" value="New audit request — SNC Advertising" />
      <input type="hidden" name="from_name" value="SNC Advertising — website" />
      <input type="checkbox" name="botcheck" class="hp" tabindex="-1" autocomplete="off" />

      <div class="field">
        <label for="name">Name</label>
        <input id="name" name="name" type="text" required autocomplete="name" />
      </div>

      <div class="row">
        <div class="field">
          <label for="email">Email</label>
          <input id="email" name="email" type="email" required autocomplete="email" />
        </div>
        <div class="field">
          <label for="telegram">Telegram <span class="opt">— optional</span></label>
          <input id="telegram" name="telegram" type="text" placeholder="@username" />
        </div>
      </div>

      <div class="field">
        <label for="company">Website or brand</label>
        <input id="company" name="company" type="text" required placeholder="example.com" />
      </div>

      <div class="field">
        <label class="field-label">Type of business</label>
        <div class="chips" role="radiogroup" aria-label="Type of business">
          <label class="chip"><input type="radio" name="business_type" value="Ecommerce" required /><span>Ecommerce</span></label>
          <label class="chip"><input type="radio" name="business_type" value="Lead-gen / services" /><span>Lead-gen / services</span></label>
          <label class="chip"><input type="radio" name="business_type" value="Other" /><span>Other</span></label>
        </div>
      </div>

      <div class="field">
        <label class="field-label">Monthly ad budget</label>
        <div class="chips" role="radiogroup" aria-label="Monthly ad budget">
          <label class="chip"><input type="radio" name="ad_budget" value="Under €1,000" required /><span>Under €1,000</span></label>
          <label class="chip"><input type="radio" name="ad_budget" value="€1,000–3,000" /><span>€1,000–3,000</span></label>
          <label class="chip"><input type="radio" name="ad_budget" value="€3,000–10,000" /><span>€3,000–10,000</span></label>
          <label class="chip"><input type="radio" name="ad_budget" value="Over €10,000" /><span>Over €10,000</span></label>
        </div>
      </div>

      <div class="field">
        <label for="message">Anything we should know <span class="opt">— optional</span></label>
        <textarea id="message" name="message" rows="4" placeholder="What's wrong with your ads right now, and what you want to hit."></textarea>
      </div>

      <button type="submit" class="btn btn-on-white form-submit">Send request</button>
      <p id="form-status" class="form-status" role="status" aria-live="polite"></p>
    </form>
  </section>

  <!-- What happens next -->
  <section class="section section-warm">
    <div class="sec-label">What happens next</div>
    <ol class="next">
      {steps.map((s, i) => (
        <li class="next-item">
          <span class="next-n">{String(i + 1).padStart(2, '0')}</span>
          <span class="next-d muted-light">{s}</span>
        </li>
      ))}
    </ol>
  </section>

  <!-- Book a call -->
  <section class="section section-light">
    <div class="sec-label">Book a call</div>
    <h2 class="statement" style="margin-top:20px;max-width:640px;">
      Already know what you want to discuss?
    </h2>
    <p class="lede muted-light" style="margin-top:16px;">
      Book 30 minutes directly — we'll review the account and answer your
      questions. Best if you're already at the decision stage.
    </p>
    <div
      class="calendly-inline-widget"
      data-url="https://calendly.com/dmitrijs-sncads/30min?hide_gdpr_banner=1&primary_color=1e4d3a"
      style="min-width:320px;height:700px;margin-top:28px;"
    >
    </div>
    <script
      is:inline
      src="https://assets.calendly.com/assets/external/widget.js"
      async
    ></script>
  </section>

  <!-- Direct contact -->
  <section class="section section-forest">
    <div class="sec-label on-dark">Prefer to skip the form?</div>
    <p class="lede muted-dark" style="margin-top:20px;">
      Message us directly.
    </p>
    <div class="direct">
      <a class="direct-link" href="https://t.me/">Telegram &rarr;</a>
      <a class="direct-link" href="mailto:hello@snc-ads.com">hello@snc-ads.com &rarr;</a>
    </div>
  </section>
</Base>

<style>
  .form { margin-top: 28px; max-width: 640px; }
  .hp { position: absolute; left: -9999px; }

  .row { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
  .field { margin-bottom: 20px; display: flex; flex-direction: column; }
  .field label {
    font-size: 10px; font-weight: 600; letter-spacing: 0.1em;
    text-transform: uppercase; color: rgba(255, 255, 255, 0.6);
    margin-bottom: 8px;
  }
  .opt { font-weight: 400; text-transform: none; letter-spacing: 0; color: var(--text-dim); }

  .field input,
  .field select,
  .field textarea {
    font-family: var(--font);
    font-size: 13px;
    color: var(--white);
    background: #141414;
    border: 1px solid var(--dim);
    border-radius: 0;
    padding: 12px 14px;
    transition: border-color 0.15s;
  }
  .field textarea { resize: vertical; }
  .field input:focus,
  .field select:focus,
  .field textarea:focus {
    outline: none; border-color: var(--sage);
  }
  .field input::placeholder,
  .field textarea::placeholder { color: var(--text-dim); }

  /* Selectable chips — minimalist replacement for the old dropdowns */
  .chips { display: flex; flex-wrap: wrap; gap: 8px; }
  .chip { position: relative; display: inline-flex; cursor: pointer; }
  .chip input { position: absolute; inset: 0; margin: 0; opacity: 0; cursor: pointer; }
  .chip span {
    font-size: 13px; line-height: 1; color: rgba(255, 255, 255, 0.7);
    background: #141414; border: 1px solid var(--dim);
    padding: 11px 16px; user-select: none;
    transition: border-color 0.15s, background 0.15s, color 0.15s;
  }
  .chip:hover span { border-color: var(--sage); }
  .chip input:checked + span { background: var(--green); border-color: var(--green); color: var(--white); }
  .chip input:focus-visible + span { outline: 1px solid var(--sage); outline-offset: 2px; }

  .form-submit { margin-top: 4px; cursor: pointer; }
  .form-submit[disabled] { opacity: 0.5; cursor: default; }

  .form-status {
    margin-top: 16px; font-size: 12px; line-height: 1.6; min-height: 1em;
  }
  .form-status.ok  { color: var(--sage); }
  .form-status.err { color: var(--bronze); }

  .next { margin-top: 28px; max-width: 680px; }
  .next-item {
    display: flex; gap: 20px; align-items: baseline;
    padding: 16px 0; border-top: 1px solid var(--dim-light);
  }
  .next-item:last-child { border-bottom: 1px solid var(--dim-light); }
  .next-n { font-size: 13px; font-weight: 600; color: var(--bronze); flex-shrink: 0; }
  .next-d { font-size: 14px; line-height: 1.7; }

  .direct { margin-top: 20px; display: flex; gap: 32px; flex-wrap: wrap; }
  .direct-link {
    font-size: 14px; font-weight: 600; color: var(--sage);
    border-bottom: 1px solid transparent; transition: border-color 0.15s;
  }
  .direct-link:hover { border-bottom-color: var(--sage); }

  @media (max-width: 640px) {
    .row { grid-template-columns: 1fr; gap: 0; }
  }
</style>

<script>
  const form = document.getElementById('audit-form') as HTMLFormElement | null;
  const status = document.getElementById('form-status');
  const submit = form?.querySelector('.form-submit') as HTMLButtonElement | null;

  if (form && status && submit) {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const key = (form.elements.namedItem('access_key') as HTMLInputElement)?.value;
      if (!key || key.startsWith('REPLACE_WITH')) {
        status.textContent =
          "The form isn't live yet — message us on Telegram or by email below.";
        status.className = 'form-status err';
        return;
      }

      status.textContent = '';
      status.className = 'form-status';
      submit.disabled = true;
      submit.textContent = 'Sending…';

      try {
        const res = await fetch('https://api.web3forms.com/submit', {
          method: 'POST',
          headers: { Accept: 'application/json' },
          body: new FormData(form),
        });
        const data = await res.json();
        if (data.success) {
          // Redirect to the thank-you page (which fires the Lead conversion).
          const redirect = form.dataset.redirect;
          if (redirect) { window.location.href = redirect; return; }
          status.textContent = "Request sent. We'll get back to you within one business day.";
          status.className = 'form-status ok';
          submit.textContent = 'Sent';
        } else {
          throw new Error(data.message || 'submit failed');
        }
      } catch {
        status.textContent =
          "Couldn't send — try again, or message us on Telegram.";
        status.className = 'form-status err';
        submit.disabled = false;
        submit.textContent = 'Send request';
      }
    });
  }
</script>
